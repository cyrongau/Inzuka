export const FEATURES = {
  // If true, the app acts as a real financial custodian (Escrow).
  // Users will use STK Push to actually move money from their M-Pesa to the Platform Paybill.
  // Requires CBK / financial compliance, Daraja webhooks, etc.
  ENABLE_REAL_MONEY_ESCROW: false, 
};
